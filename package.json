{
  "name": "autoprefixer-transform",
  "version": "0.0.0",
  "description": "Autoprefixer transform stream",
  "main": "dist/transform.js",
  "scripts": {
    "test": "tape ./spec/*.js",
    "build": "rm -rf dist && babel lib --out-dir dist",
    "watch": "babel --watch lib --out-dir dist",
    "prepublish": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/mattstyles/autoprefixer-transform.git"
  },
  "keywords": [
    "autoprefixer",
    "css",
    "transform",
    "pipe",
    "pipeline"
  ],
  "author": "Matt Styles",
  "license": "WTFPL",
  "bugs": {
    "url": "https://github.com/mattstyles/autoprefixer-transform/issues"
  },
  "homepage": "https://github.com/mattstyles/autoprefixer-transform",
  "dependencies": {},
  "devDependencies": {
    "babel": "^5.1.8",
    "tape": "^4.0.0"
  }
}
